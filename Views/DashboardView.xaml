<UserControl x:Class="WPF9SimpleMesMonitorSystem.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
             mc:Ignorable="d">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="Dashboard" FontSize="20" FontWeight="Bold" Margin="0 0 0 12" />

        <Grid Grid.Row="1" Margin="0 0 0 12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Background="White" Padding="8" CornerRadius="4" BorderBrush="#DDDDDD" BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="Device Status" FontWeight="SemiBold" Margin="0 0 0 8" />
                    <lvc:PieChart Series="{Binding DeviceStatusSeries}" LegendPosition="Right" />
                </StackPanel>
            </Border>

            <Border Grid.Column="1" Background="White" Padding="8" CornerRadius="4" BorderBrush="#DDDDDD" BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="Order Progress" FontWeight="SemiBold" Margin="0 0 0 8" />
                    <lvc:CartesianChart Series="{Binding OrderProgressSeries}" XAxes="{Binding OrderXAxes}" LegendPosition="Right" />
                </StackPanel>
            </Border>
        </Grid>

        <Border Grid.Row="2" Background="White" Padding="8" CornerRadius="4" BorderBrush="#DDDDDD" BorderThickness="1">
            <StackPanel>
                <TextBlock Text="Parameter Trend" FontWeight="SemiBold" Margin="0 0 0 8" />
                <lvc:CartesianChart Series="{Binding ParameterSeries}" XAxes="{Binding ParameterXAxes}" LegendPosition="Right" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
